version: 2
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: docker login -u $DOCKER_USER -p $DOCKER_PASS
      - run: docker build -t dorelio/dorel-spa-test:$CIRCLE_BRANCH .
      - run: docker push dorelio/dorel-spa-test:$CIRCLE_BRANCH
workflows:
  version: 2
  build_and_test:
    jobs:
      - build:
          context: dorelio
